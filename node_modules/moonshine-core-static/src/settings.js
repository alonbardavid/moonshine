module.exports.config = function(settings) {
    settings.STATIC_ROOTDIR = "static"
    settings.STATIC_ROUTES = {
        "/static":"/", //route on server: prefix in filesystem
        "/":"/page"
    }
}
module.exports.requiredComponents = ["moonshine-core-server"]