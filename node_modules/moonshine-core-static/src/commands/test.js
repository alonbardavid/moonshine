var moonshine = require("moonshine-core"),
    util = require("util"),
    OriginalCommand = require("moonshine-core-server/src/commands/test")
    settings = moonshine.settings,
    logger = moonshine.logFactory(),
    spawn = require("child_process").spawn,
    when = require("when"),
    path = require("path")

function TestCommand(){

}
util.inherits(TestCommand,OriginalCommand)

TestCommand.prototype.configKarma = function(config){
    var files = []
    for(var key in moonshine.static.files) {
        files.push(moonshine.static.files[key]);
    }
    config.files = files.concat(config.files)
}
module.exports = TestCommand;